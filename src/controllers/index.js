const userController = require('./user.controller');
const categoryControler = require('./category.controller');
const postController = require('./post.controller');

module.exports = {
    userController,
    postController,
    categoryControler,
};